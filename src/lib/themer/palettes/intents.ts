import * as colors from './colors';
import * as grays from './grays';

const { neutral, greenish, slate, gray, zinc, stone, indigray, cyanish } = grays;

export const primary = colors;
export const secondary = colors;
export const accent = colors;

export const grayColors = {
    neutral,
    greenish,
    slate,
    gray,
    zinc,
    stone,
    indigray,
    cyanish
};

export const danger = {
    red: colors.red,
    rose: colors.rose
};

export const success = {
    lime: colors.lime,
    green: colors.green,
    teal: colors.teal,
    emerald: colors.emerald
};

export const warning = {
    amber: colors.amber,
    yellow: colors.yellow
};

export const info = {
    cyan: colors.cyan,
    sky: colors.sky,
    blue: colors.blue
};