export const checkboxProps = {
    head: {
        cells: ['Prop', 'Type', 'Default']
    },
    rows: [
        {
            cells: ['intent', `"primary" | "secondary" | "accent" | "gray" | "neutral"`, 'primary']
        },
        {
            cells: ['fancy', `boolean`, 'false']
        }
    ]
};

export const alignerProps = {
    head: {
        cells: ['Prop', 'Type', 'Default']
    },
    rows: [
        {
            cells: ['fromRight', 'boolean', 'false']
        }
    ]
};
