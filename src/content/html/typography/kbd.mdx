---
title: Kbd
description: Useful for showcasing keyboard shortcuts or other keyboard-based commands.
priorityOrder : 25
---

import _components from "@content/_components"
import CodeSnippet from "@components/utilities/CodeSnippet"
import ComponentPreview from "@components/ComponentPreview"
import { Kbd } from "@tailus-ui/typography"
import DocsStackedSnippets from "@components/DocsStackedSnippets"

export const components = {
    ..._components
}

export const componentCode = `import React from "react"
import { kbdTheme } from "@tailus/themer"

export interface KbdProps extends React.HTMLAttributes<HTMLElement>{}
    
export const Kbd = React.forwardRef<HTMLPreElement, KbdProps>(({
    children,
    className,
    ...props
}, ref) => {
    return (
        <kbd ref={ref} className={kbdTheme({className })} {...props}>
            {children}
        </kbd>
    )
})`

export const component = {
    code : componentCode,
    lang:"tsx",
    file:"tsx",
    filename:"Kbd.tsx",
}

export const usage = [
  {
    code: `import { Kbd } from "@tailus-ui/typography";`,
    lang: "tsx",
  },
  {
    code: `const MyComponent = () => (
    <Kbd>Cmd</Kbd>
)`,
    lang: "tsx",
  }
]

<div className="space-y-20">
    <ComponentPreview>
        <Kbd>⌘ K</Kbd>
    </ComponentPreview>
    <div>
        ## Installation
        Copy-Paste the component code in a `.tsx` file.

        <CodeSnippet {...component} client:visible />
    </div>

    <div>
        ## Usage
        Import the `<Kbd/>` and start building.
        <DocsStackedSnippets snippets={usage} client:visible />
    </div>
</div>