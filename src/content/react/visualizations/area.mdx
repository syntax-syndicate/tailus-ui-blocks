---
title: Area
description: Learn how to apply styles to your Area component in Recharts
priorityOrder : 65
frameworkDocs : 
    name : Recharts
    link : https://recharts.org/en-US/api/bar
---

import _components from "@content/_components"
import CodeSnippet from "@components/utilities/CodeSnippet"
import {
    AreaChartOverview,
    AreaGradientChart,
    areaGradientCode,
    areaStyleProps
} from "@react-ui/visualizations"
import ComponentPreview from "@components/ComponentPreview"
import Table from "@components/Table.astro"
import PreviewBlock from "@components/PreviewBlock.astro"

export const components = {
    ..._components
}

export const componentCode = `import React from 'react';
import { gradientStop, type AreaProps as AreaVariants } from "@tailus/themer"

export const LinearGradient: React.FC<Omit<AreaVariants, "gradient"> & {id:string}> = ({intent, id}) => {
    return (
        <linearGradient x1={0} y1={0} x2={0} y2={1} id={id}>
            <stop className={gradientStop({intent})} offset="5%" stopColor="currentColor" stopOpacity={0.8} />
            <stop className="text-white dark:text-gray-950" offset="95%" stopColor="currentColor" stopOpacity={0} />
        </linearGradient>
    )
}`
export const component = {
    code : componentCode,
    lang:"tsx",
}

export const importation = {
    code: `import { Area } from 'recharts';
import { area } from "@tailus/themer";`,
    lang : "tsx"
}


export const applyStyles = {
    code: `<Area 
    className={area({ intent: "primary" })}
    fill="currentColor"
    stroke="currentColor"
    activeDot={{ color: "var(--area-primary-stroke)", r: 4, stroke: "white" }}
    dataKey="Primary"
/>`,
        lang: "tsx",
    }

<div className="space-y-20">
    <ComponentPreview >
        <AreaChartOverview client:visible />
    </ComponentPreview>
    <div>
        ## Installation

        No installation is required for the Area component. You can use it directly in your project.
    </div>

    <div>
        ## Usage
        
        Import the `<Area />` component from the recharts library and the `area` styles from `@tailus/themer`.
        
        <CodeSnippet {...importation} client:visible/>

        To change the color of the `<Area />` component, set the `fill` and `stroke` props to `currentColor` and apply the `area` styles to the `className` prop.
        
        <CodeSnippet {...applyStyles} client:visible/>
    </div>

    <div>
        ## Reference

        Although we didn't create a custom Area component, you can change the component's color using the `intent` variant of the `area` styles.

        <Table data={areaStyleProps} />
    </div>

    <div>
        ## Examples

        <div>
            ### Custom Linear Gradient

            <PreviewBlock codeBlocks={areaGradientCode}>
                <AreaGradientChart client:visible/>
            </PreviewBlock>
        </div>

        See more examples on the [AreaChart](/react/visualizations/area-chart) page
    </div>
</div>