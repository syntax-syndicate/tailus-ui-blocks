---
title: Toast
description: A succinct message that is displayed temporarily.
priorityOrder : 56
frameworkDocs : 
    name : Radix UI
    link : https://www.radix-ui.com/primitives/docs/components/toast#api-reference
---

import _components from "@content/_components"
import DocsStackedSnippets from "@components/DocsStackedSnippets"
import ComponentPreview from "@components/ComponentPreview"
import PreviewBlock from "@components/PreviewBlock.astro"
import CodeSnippet from "@components/utilities/CodeSnippet"
import Table from "@components/Table.astro"

import { Overview } from "@react-ui/toast/Overview"
import { rootProps } from "@react-ui/toast/reference"
import { installation, overview } from "@react-ui/toast/codeSnippets"

export const components = {
    ..._components
}

export const usage = {
        code: `import Toast from '@tailus-ui/Toast';

export default () => (
  <Toast.Provider>
    <Toast.Root>
      <Toast.Title />
      <Toast.Description />
      <Toast.Action />
      <Toast.Close />
    </Toast.Root>

    <Toast.Viewport />
  </Toast.Provider>
);`,
        lang: "tsx",
}


<div className="space-y-20">

    <PreviewBlock
        codeBlocks={overview}
    >
        <Overview client:visible/>
    </PreviewBlock>

    <div>
        ## Installation

        Install the primitive and Copy-Paste the component code in a `.tsx` file.

        <DocsStackedSnippets snippets={installation} client:visible/>
    </div>

    <div>
        ## Usage

        Import all the parts and build your Toast.
        
        <CodeSnippet {...usage} client:visible/>
    </div>

    <div>
        ## Reference

        <div className="mt-6 mb-12 space-y-12"> 

            <div>
                ### Root
                
                The Slider `<Root />` has the following additional props.

                <Table data={rootProps}/>
            </div>
        </div>
    </div>

    <div>
        ## Examples

        <div className="space-y-12">
            <div>
                
            </div>
        </div>
    </div>
</div>