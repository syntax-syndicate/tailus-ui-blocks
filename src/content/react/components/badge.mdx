---
title: Badge
description: A Badge is a small component that is typically used to display status information or additional context for another UI element.
priorityOrder : 31
---
import _components from "@content/_components"
import DocsStackedSnippets from "@components/DocsStackedSnippets"
import ComponentPreview from "@components/ComponentPreview"
import PreviewBlock from "@components/PreviewBlock.astro"
import CodeSnippet from "@components/utilities/CodeSnippet"
import Table from "@components/Table.astro"
import Badge from "@react-ui/badge/Badge"
import { badgeProps } from "@react-ui/badge/reference"
import { Intents, intentsCode } from "@react-ui/badge/Intents"
import { Sizes, sizesCode } from "@react-ui/badge/Sizes"

export const components = {
    ..._components
}

export const componentCode = {
    code: `import { badge, type BadgeProps } from "@tailus/themer"

export interface BadgeVariantsProps extends React.HTMLAttributes<HTMLDivElement>, BadgeProps {}

export const Badge: React.FC<BadgeVariantsProps> = ({
    className,
    children,
    intent = "primary", 
    size = "md",
    variant = "soft",
    ...props
  }) => {
    return(
        <span className={badge[variant]({intent, size, className})} {...props}>
            {children}
        </span>
    )
}
  
Badge.displayName = "Badge";
export default Badge;`,
    lang: "tsx"
}

export const usage = [
    {
        code: `import Badge from "@tailus-ui/Badge";`,
        lang: "tsx",
    },
    {
        code: `const MyComponent = () => (
    <Badge size="sm" intent="danger">Badge</Badge>
)`,
        lang: "tsx",
    }
]

<div className="space-y-20">
    <ComponentPreview>  
        <Badge size="sm" intent="danger">Badge</Badge>     
    </ComponentPreview>

    <div>
        ## Installation

        Copy-Paste the component code in a `.tsx` file.

        <CodeSnippet {...componentCode} client:visible/>
    </div>

    <div>
        ## Usage

        Import the `Badge` component and use it in your component.

        <DocsStackedSnippets snippets={usage} client:visible/>
    </div>

    <div>
        ## Reference

        <div className="mt-6 mb-12"> 
            ### Props
            
            The Badge component has the following props.

            <Table data={badgeProps}/>
        </div>
    </div>

    <div>
        ## Examples

        <div className="space-y-12">
            <div>
                ### Colors 

                <PreviewBlock
                    codeBlocks={intentsCode}
                >
                    <Intents client:visible/>
                </PreviewBlock>
            </div>

            <div>
                ### Sizes

                <PreviewBlock
                    codeBlocks={sizesCode}
                >
                    <Sizes client:visible/>
                </PreviewBlock>
            </div>
        </div>
    </div>
</div>