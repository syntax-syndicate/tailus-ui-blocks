---
title: Empty State
description: Also known as a blank state, the empty state is basically a screen you encounter when there's no data to display.
priorityOrder : 42
---

import _components from "@content/_components"
import PreviewBlock from "@components/PreviewBlock.astro"

import Button from "@react-ui/button/Button";
import { Plus } from "lucide-react";
import { Title, Text, Caption } from "@components/typography"
import { ChatIllustration, ContentIllustration } from "@components/examples/Illustrations";
import Separator from "@react-ui/separator/Separator"
import Card from "@react-ui/card/Card"
import Avatar from "@react-ui/avatar/Avatar"
import { BERNARD_AVATAR, MESCHAC_AVATAR, THEO_AVATAR } from "src/const";

export const components = {
    ..._components
}

export const importation = `import Button from "@tailus-ui/Button";
import { Plus } from "lucide-react";
import { Title, Text } from "@tailus-ui/typography"`

<div className="space-y-20">
    <div>
    ## Example 1
    <PreviewBlock
        codeBlocks={[{
            code: `${importation}

const MyComponent = () => (
    <div className="max-w-sm mx-auto space-y-6">
        <ContentIllustration />
        <div className="space-y-2 text-center">
            <Title size="lg">You don't seem to have any file</Title>
            <Text>Basically a screen you encounter when there's no data to display.</Text>
        </div>
        <Button.Root className="mx-auto" variant="outlined" intent="gray">
            <Button.Icon type="leading" size="md">
                <Plus />
            </Button.Icon>
            <Button.Label>Add a file</Button.Label>
        </Button.Root>
    </div>
)`,
            lang : "tsx"
        }]}
    >
        <div className="max-w-sm mx-auto space-y-6">
            <ContentIllustration />
            <div className="space-y-2 text-center">
            <Title size="lg">You don't seem to have any file</Title>
            <Text className="mx-auto">
                Basically a screen you encounter when there's no data to display.
            </Text>
            </div>
            <Button.Root className="mx-auto" variant="outlined" intent="gray">
            <Button.Icon type="leading" size="md" client:visible><Plus className="size-4"/></Button.Icon>
            <Button.Label>Add a file</Button.Label>
            </Button.Root>
        </div>
    </PreviewBlock>
    </div>

    <div>
        ## Example 2
        <PreviewBlock
        codeBlocks={[{
            code: `${importation}

const MyComponent = () => (
    <div className="max-w-sm mx-auto space-y-6">
        <ChatIllustration />
        <div className="space-y-2 text-center">
            <Title size="lg">Add friends to start a discussion</Title>
            <Text>Basically a screen you encounter when there's no data to display.</Text>
        </div>
        <Button.Root className="mx-auto">
            <Button.Label>Browse friends</Button.Label>
        </Button.Root>
    </div>
)`,
            lang : "tsx"
        }]}
    >
        <div className="mt-12 max-w-lg w-full mx-auto">
            <ChatIllustration />
            <div className="mt-6 space-y-2 text-center">
                <Title size="lg">Add friends to start a discussion</Title>
                <Text className="my-0 max-w-xs mx-auto">Basically a screen you encounter when there's no data to display.</Text>
            </div>
            <div className="mt-12 grid grid-cols-2 sm:grid-cols-3 gap-3">
                <Card className="p-2">
                    <img className="mt-2 size-16 mx-auto rounded-full" src={MESCHAC_AVATAR} width={400} height={400} loading="lazy" />
                    <div className="my-3">
                        <Title size="base" as="p" weight="medium" align="center">Méschac Irung</Title>
                        <Caption align="center">Suggested for you</Caption>
                    </div>
                    <Button.Root className="w-full" variant="soft" size="sm" data-rounded="medium">
                        <Button.Label>Add friend</Button.Label>
                    </Button.Root>
                </Card>
                <Card className="p-2">
                    <img className="mt-2 size-16 mx-auto rounded-full" src={BERNARD_AVATAR} width={400} height={400} loading="lazy" />
                    <div className="my-3">
                        <Title size="base" as="p" weight="medium" align="center">Bernard Ng</Title>
                        <Caption align="center">Suggested for you</Caption>
                    </div>
                    <Button.Root className="w-full" variant="soft" size="sm" data-rounded="medium">
                        <Button.Label>Add friend</Button.Label>
                    </Button.Root>
                </Card>
                <Card className="p-2">
                    <img className="mt-2 size-16 mx-auto rounded-full" src={THEO_AVATAR} width={400} height={400} loading="lazy" />
                    <div className="my-3">
                        <Title size="base" as="p" weight="medium" align="center">Théo Balick</Title>
                        <Caption align="center">Suggested for you</Caption>
                    </div>
                    <Button.Root className="w-full" variant="soft" size="sm" data-rounded="medium">
                        <Button.Label>Add friend</Button.Label>
                    </Button.Root>
                </Card>
            </div>
        </div>
    </PreviewBlock>
    </div>
</div>