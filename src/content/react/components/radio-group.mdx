---
title: Radio Group
description: A set of checkable buttons—known as radio buttons—where no more than one of the buttons can be checked at a time.
priorityOrder : 48
frameworkDocs : 
    name : Radix UI
    link : https://www.radix-ui.com/primitives/docs/components/radio-group#api-reference
---

import _components from "@content/_components"
import DocsStackedSnippets from "@components/DocsStackedSnippets"
import ComponentPreview from "@components/ComponentPreview"
import PreviewBlock from "@components/PreviewBlock.astro"
import CodeSnippet from "@components/utilities/CodeSnippet"
import Table from "@components/Table.astro"

import { Overview, overviewCode } from "@react-ui/radio-group/Overview"
import { checkboxProps, alignerProps } from "@react-ui/checkbox/reference"
import { installation } from "@react-ui/radio-group/codeSnippets"
import { aligner } from "@react-ui/checkbox/codeSnippets"

export const components = {
    ..._components
}

export const usage = [
    {
        code: `import RadioGroup from "@tailus-ui/RadioGroup";
import Label from "@tailus-ui/Label";`,
        lang: "tsx",
    },
    {
        code: `const MyComponent = () => (
    <div>
        <RadioGroup.Root defaultValue="problem" aria-label="User feedback">
            <RadioGroup.Item value="easy" id="easy">
                <RadioGroup.Indicator/>
            </RadioGroup.Item>
            <Label htmlFor="easy">
                Easy understanding
            </Label>
        </RadioGroup.Root>
    </div>
)`,
        lang: "tsx",
    }
]

<div className="space-y-20">

    <PreviewBlock codeBlocks={overviewCode}>
        <Overview client:visible/>
    </PreviewBlock>

    <div>
        ## Installation

        Install the primitive and Copy-Paste the component code in a `.tsx` file.

        <DocsStackedSnippets snippets={installation} client:visible/>

        <div className="mt-12">

            ### Add Aligner component

            Useful for aligning `Checkbox`, `RadioGroup`, and `Switch` components with their associated `Label` and `Caption`

            <CodeSnippet {...aligner} client:visible/>
        </div>
    </div>

    <div>
        ## Usage

        Import all the parts and build your Radio Group.
        
        <DocsStackedSnippets snippets={usage} client:visible/>
    </div>

    <div>
        ## Reference

        <div className="mt-6 mb-12 space-y-12"> 
        
            <div>
                ### Root

                The Radio Group `<Root />` has the following additional props.

                <Table data={checkboxProps}/>
            </div>
            
            <div>
                ### Aligner
                
                The Aligner component has the following props.

                <Table data={alignerProps}/>
            </div>
        </div>
    </div>

    <div>
        ## Examples

        <div className="space-y-12">
            <div>
                
            </div>
        </div>
    </div>
</div>