---
title: Dialog
description: A window overlaid on either the primary window or another dialog window, rendering the content underneath inert.
priorityOrder : 39
frameworkDocs : 
    name : Radix UI
    link : https://www.radix-ui.com/primitives/docs/components/dialog#api-reference
---

import _components from "@content/_components"
import DocsStackedSnippets from "@components/DocsStackedSnippets"
import ComponentPreview from "@components/ComponentPreview"
import PreviewBlock from "@components/PreviewBlock.astro"
import CodeSnippet from "@components/utilities/CodeSnippet"
import Table from "@components/Table.astro"

import { Overview, overviewCode } from "@react-ui/dialog/Overview"
import { NewProject, newProjectCode } from "@react-ui/dialog/NewProject"
import { NewProject2, newProject2Code } from "@react-ui/dialog/NewProject2"
import { RepositoryEdit, repoEditCode} from "@react-ui/dialog/RepoEdit"
import { ProfileEdit, profileCode } from "@react-ui/dialog/ProfileEdit"
import { NewTask, taskCode } from "@react-ui/dialog/Task"
import {reference} from "@react-ui/alert-dialog"
import { installation, usage } from "@react-ui/dialog/codeSnippets"

export const components = {
    ..._components
}

<div className="space-y-20">

    <PreviewBlock
        codeBlocks={overviewCode}
    >
        <Overview client:visible/>
    </PreviewBlock>


    <div>
        ## Installation

        Install the primitive and Copy-Paste the component code in a `.tsx` file.

        <DocsStackedSnippets snippets={installation} client:visible/>
    </div>

    <div>
        ## Usage

        Import all the parts and build your Dialog.
        
        <DocsStackedSnippets snippets={usage} client:visible/>
    </div>

    <div>
        ## Reference

        <div className="mt-6 space-y-12 mb-12"> 
            <div>
                ### Content 

                The content of the `Dialog` component

                <Table data={reference.contentProps} />
            </div>

            <div>
                ### Title

                The title of the `Dialog` component

                <Table data={reference.titleProps} />
            </div>

            <div>
                ### Description

                The description of the `Dialog` component

                <Table data={reference.descriptionProps} />
            </div>

            <div>
                ### Actions

                The parent component of the action buttons.
            </div>
        </div>
    </div>

    <div>
        ## Examples

        <div className="space-y-12">
            <div>
                ### New project

                <PreviewBlock codeBlocks={newProjectCode}>
                    <NewProject client:visible/>
                </PreviewBlock>
            </div>

            <div>
                ### New project 2

                <PreviewBlock codeBlocks={newProject2Code}>
                    <NewProject2 client:visible/>
                </PreviewBlock>
            </div>

            <div>
                ### Edit Repository

                <PreviewBlock codeBlocks={repoEditCode}>
                    <RepositoryEdit client:visible/>
                </PreviewBlock>
            </div>

            <div>
                ### Edit Profile

                <PreviewBlock codeBlocks={profileCode}>
                    <ProfileEdit client:visible/>
                </PreviewBlock>
            </div>

            <div>
                ### New Task

                <PreviewBlock codeBlocks={taskCode}>
                    <NewTask client:load/>
                </PreviewBlock>
            </div>
        </div>
    </div>
</div>