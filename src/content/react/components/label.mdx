---
title: Label
description: Renders an accessible label associated with controls.
priorityOrder : 45
frameworkDocs : 
    name : Radix UI
    link : https://www.radix-ui.com/primitives/docs/components/label#api-reference
---

import _components from "@content/_components"
import DocsStackedSnippets from "@components/DocsStackedSnippets"
import ComponentPreview from "@components/ComponentPreview"
import PreviewBlock from "@components/PreviewBlock.astro"
import CodeSnippet from "@components/utilities/CodeSnippet"
import Table from "@components/Table.astro"

import { Overview } from "@react-ui/label/Overview"
import { RadioCards, radioCardsCode } from "@react-ui/label/RadioCard"
import { CheckboxCards, checkCardsCode } from "@react-ui/label/CheckCard"

import { labelProps } from "@react-ui/label/reference"
import { installation } from "@react-ui/label/codeSnippets"

export const components = {
    ..._components
}

export const usage = [
    {
        code: `import Label from "@tailus-ui/Label";
import Input from "@tailus-ui/Input";`,
        lang: "tsx",
    },
    {
        code: `const MyComponent = () => (
    <div className="space-y-2">
        <Label size="md" htmlFor="name">Full Name</Label>
        <Input id="name"/>
    </div>
)`,
        lang: "tsx",
    }
]

<div className="space-y-20">

    <ComponentPreview>
        <Overview client:visible/>
    </ComponentPreview>

    <div>
        ## Installation

        Install the primitive and Copy-Paste the component code in a `.tsx` file.

        <DocsStackedSnippets snippets={installation} client:visible/>
    </div>

    <div>
        ## Usage

        Import all the parts and build your Component.
        
        <DocsStackedSnippets snippets={usage} client:visible/>
    </div>

    <div>
        ## Reference

        <div className="mt-6 mb-12"> 
            ### Label
            
            The label component

            <Table data={labelProps}/>
        </div>
    </div>

    <div>
        ## Examples

        <div className="space-y-12">
            <div>
                ### Radio Cards
                <PreviewBlock
                    codeBlocks={radioCardsCode}
                >
                    <RadioCards client:visible/>
                </PreviewBlock>
            </div>
            <div>
                ### Checkbox Cards
                <PreviewBlock
                    codeBlocks={checkCardsCode}
                >
                    <CheckboxCards client:visible/>
                </PreviewBlock>
            </div>
        </div>
    </div>
</div>