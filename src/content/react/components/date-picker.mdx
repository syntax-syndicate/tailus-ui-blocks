---
title: Date Picker
description: The Date Picker component is used to select a date.
priorityOrder : 38
frameworkDocs : 
    name : Calendar
    link : /react/components/calendar
---
import _components from "@content/_components"
import ComponentPreview from "@components/ComponentPreview"
import PreviewBlock from "@components/PreviewBlock.astro"
import { DatePicker, datePickerCode } from "@react-ui/date-picker/Basic"
import { WithPresets, withPresetsCode } from "@react-ui/date-picker/WithPresets"
import { WithPresetsInputs, withPresetsInputsCode } from "@react-ui/date-picker/WithPresetsInputs"

export const components = {
    ..._components
}

<div className="space-y-20">
    <PreviewBlock
        codeBlocks={datePickerCode}
    >
        <DatePicker client:idle/>
    </PreviewBlock>

    <div>
        ## With Presets
        <PreviewBlock
            codeBlocks={withPresetsCode}
        >
            <WithPresets client:idle/>
        </PreviewBlock>
    </div>

    <div>
        ## With Presets and Inputs
        <PreviewBlock
            codeBlocks={withPresetsInputsCode}
        >
            <WithPresetsInputs client:idle/>
        </PreviewBlock>
    </div>
</div>