---
title: Dropdown Menu
description: Displays a menu located at the pointer, triggered by a right-click or a long-press.
priorityOrder : 41
frameworkDocs : 
    name : Radix UI
    link : https://www.radix-ui.com/primitives/docs/components/dropdown-menu#api-reference
---
import Table from "@components/Table.astro"
import _components from "@content/_components"
import CodeSnippet from "@components/utilities/CodeSnippet"
import DocsStackedSnippets from "@components/DocsStackedSnippets"
import ComponentPreview from "@components/ComponentPreview"
import PreviewBlock from "@components/PreviewBlock.astro"
import {UserDropdown} from "@components/examples/UserDropdown"

import {
    Overview,
    overviewCode,
    installation,
    usage,
    reference
} from "@react-ui/dropdown"

export const components = {
    ..._components
}

<div className="space-y-20">
    <PreviewBlock
        codeBlocks={overviewCode}
    >
        <Overview client:visible/>
    </PreviewBlock>

    <div>
        ## Installation

        Install the primitive and Copy-Paste the component code in a `.tsx` file.
        <DocsStackedSnippets snippets={installation} client:visible />
    </div>

    <div>
        ## Usage

        Import the `ContextMenu` parts you need to use in your component and build your Context Menu.

        <DocsStackedSnippets
            snippets={usage}
            client:visible
        />
    </div>

    <div>
        ## Reference

        <div className="mt-6 space-y-12 mb-12"> 
            <div>
                ### Content 

                The content of the `DropdownMenu` component

                <Table data={reference.contentProps} />
            </div>

            <div>
                ### SubContent 

                The sub content of the `DropdownMenu` component

                <Table data={reference.contentProps} />
            </div>

            <div>
                ### Item

                The item of the `DropdownMenu` component

                <Table data={reference.itemProps} />
            </div>

            <div>
                ### Icon 

                The icon of the `Item` component
            </div>
        </div>
    </div>

    <div>
        ## Examples

        <div className="space-y-12">
            ### User Profile

            <ComponentPreview>
                <UserDropdown client:visible/>
            </ComponentPreview>
        </div>
    </div>
</div>