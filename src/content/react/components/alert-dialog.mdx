---
title: Alert Dialog
description: A modal dialog that interrupts the user with important content and expects a response.
priorityOrder : 27
frameworkDocs : 
    name : Radix UI
    link : https://www.radix-ui.com/primitives/docs/components/alert-dialog#api-reference
---
import Table from "@components/Table.astro"
import _components from "@content/_components"
import CodeSnippet from "@components/utilities/CodeSnippet"
import DocsStackedSnippets from "@components/DocsStackedSnippets"
import ComponentPreview from "@components/ComponentPreview"
import PreviewBlock from "@components/PreviewBlock.astro"
import {
    installation,
    reference,
    Overview,
    Centered,
    IOs,
    overviewCode,
    centeredCode,
    iOsCode,
    Mixed,
    mixedCode,
    Fancy,
    fancyCode,
    Glassy,
    glassyCode
} from "@react-ui/alert-dialog"

export const components = {
    ..._components
}

export const guide = {
    code : `import AlertDialog from "@tailus-ui/AlertDialog"
import Button from "@tailus-ui/Button"`,
    lang : "tsx"
}

<div className="space-y-20">
    <PreviewBlock
        codeBlocks={overviewCode}
    >
        <Overview client:visible/>
    </PreviewBlock>

    <div>
        ## Installation

        Install the primitive and Copy-Paste the component code in a `.tsx` file.
        <DocsStackedSnippets snippets={installation} client:visible />
    </div>

    <div>
        ## Usage

        Import the `AlertDialog` parts you need to use in your component and build your AlertDialog.
        <CodeSnippet
            {...guide}
            client:visible
        />

        See [examples](#examples) below for more use cases.
    </div>

    <div>
        ## Reference

        <div className="mt-6 space-y-12 mb-12"> 
            <div>
                ### Content 

                The content of the `AlertDialog` component

                <Table data={reference.contentProps} />
            </div>

            <div>
                ### Title

                The title of the `AlertDialog` component

                <Table data={reference.titleProps} />
            </div>

            <div>
                ### Description

                The description of the `AlertDialog` component

                <Table data={reference.descriptionProps} />
            </div>

            <div>
                ### Actions

                The parent component of the `<Cancel />` and `<Action />` parts.
            </div>
        </div>
    </div>

    <div>
        ## Examples

        <div className="space-y-12">
            <div> 
                ### Centered
                <PreviewBlock
                    codeBlocks={centeredCode}
                >
                    <Centered client:visible/>
                </PreviewBlock>
            </div>

            <div> 
                ### iOS
                <PreviewBlock
                    codeBlocks={iOsCode}
                >
                    <IOs client:visible/>
                </PreviewBlock>
            </div>

            <div> 
                ### Mixed Content
                <PreviewBlock
                    codeBlocks={mixedCode}
                >
                    <Mixed client:visible/>
                </PreviewBlock>
            </div>

            <div> 
                ### Fancy Content
                <PreviewBlock
                    codeBlocks={fancyCode}
                >
                    <Fancy client:visible/>
                </PreviewBlock>
            </div>

            <div> 
                ### Glassy Content
                <PreviewBlock
                    codeBlocks={glassyCode}
                >
                    <Glassy client:visible/>
                </PreviewBlock>
            </div>
        </div>
    </div>
</div>