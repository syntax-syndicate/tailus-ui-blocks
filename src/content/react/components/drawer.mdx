---
title: Drawer
description: A Dialog that slides in from the edge of the screen.
priorityOrder : 40
frameworkDocs : 
    name : Vaul
    link : https://github.com/emilkowalski/vaul
---

import _components from "@content/_components"
import DocsStackedSnippets from "@components/DocsStackedSnippets"
import ComponentPreview from "@components/ComponentPreview"
import PreviewBlock from "@components/PreviewBlock.astro"
import CodeSnippet from "@components/utilities/CodeSnippet"
import Table from "@components/Table.astro"

import { Overview, overviewCode } from "@react-ui/drawer/Overview"
import * as reference from "@react-ui/drawer/reference"
import { installation, usage } from "@react-ui/drawer/codeSnippets"

export const components = {
    ..._components
}

<div className="space-y-20">

    <PreviewBlock
        codeBlocks={overviewCode}
    >
        <Overview client:visible/>
    </PreviewBlock>


    <div>
        ## Installation

        Install the primitive and Copy-Paste the component code in a `.tsx` file.

        <DocsStackedSnippets snippets={installation} client:visible/>
    </div>

    <div>
        ## Usage

        Import all the parts and build your Drawer.
        
        <DocsStackedSnippets snippets={usage} client:visible/>
    </div>

    <div>
        ## Reference

        <div className="mt-6 mb-12 space-y-12"> 
            <div>
                ### Content 

                The content of the `Drawer` component

                <Table data={reference.contentProps} />
            </div>

            <div>
                ### Title

                The title of the `Drawer` component

                <Table data={reference.titleProps} />
            </div>

            <div>
                ### Description

                The description of the `Drawer` component

                <Table data={reference.descriptionProps} />
            </div>
        </div>
    </div>

    <div>
        ## Examples

        <div className="space-y-12">
            <div>
                
            </div>
        </div>
    </div>
</div>