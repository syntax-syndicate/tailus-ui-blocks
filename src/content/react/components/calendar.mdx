---
title: Calendar
description: The Calendar component is used to display a calendar.
priorityOrder : 34
frameworkDocs : 
    name : React Day Picker
    link : https://react-day-picker.js.org/
---
import _components from "@content/_components"
import DocsStackedSnippets from "@components/DocsStackedSnippets"
import ComponentPreview from "@components/ComponentPreview"
import PreviewBlock from "@components/PreviewBlock.astro"
import CodeSnippet from "@components/utilities/CodeSnippet"
import Table from "@components/Table.astro"
import { installation } from "@react-ui/calendar/codeSnippets"
import { calendarProps } from "@react-ui/calendar/reference"
import { Overview } from "@react-ui/calendar/Overview"
import { Range } from "@react-ui/calendar/Range"
import { HandDrawn } from "@react-ui/calendar/HandDrawn"
import { RTL } from "@react-ui/calendar/RTL"

export const components = {
    ..._components
}

export const usage = [
    {
        code: `import Card from "@tailus-ui/Card";
import Calendar from "@tailus-ui/Calendar";`,
        lang: "tsx",
    },
    {
        code: `const MyComponent = () => (
    <Card>
       <Calendar
            fromYear={2024}
            toYear={2025}
       />
    </Card>
)`,
        lang: "tsx",
    }
]

<div className="space-y-20">

    <ComponentPreview>
        <Overview client:visible/>
    </ComponentPreview>

    <div>
        ## Installation

        Install the primitive and Copy-Paste the component code in a `.tsx` file.

        <DocsStackedSnippets snippets={installation} client:visible/>
    </div>

    <div>
        ## Usage

        Import the `Calendar` component and use it in your component.

        <DocsStackedSnippets snippets={usage} client:visible/>
    </div>

    <div>
        ## Reference

        <div className="mt-6 mb-12"> 
            ### Props
            
            The Calendar component has the following props.

            <Table data={calendarProps}/>
        </div>
    </div>

    <div>
        ## Examples

        <div className="space-y-12">
            <div>
                ### Range Calendar
                <br/>
                <ComponentPreview>
                    <Range client:visible/>
                </ComponentPreview>
            </div>
            
            <div>
                ### Hand Drawn Indicator
                <br/>
                <ComponentPreview>
                    <HandDrawn client:visible/>
                </ComponentPreview>
            </div>

            <div>
                ### Right to Left Calendar
                <br/>
                <ComponentPreview>
                    <RTL client:visible/>
                </ComponentPreview>
            </div>
        </div>
    </div>
</div>