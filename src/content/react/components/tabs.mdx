---
title: Tabs
description: A set of layered sections of content—known as tab panels—that are displayed one at a time.
priorityOrder : 54
frameworkDocs : 
    name : Radix UI
    link : https://www.radix-ui.com/primitives/docs/components/tabs#api-reference
---

import _components from "@content/_components"
import DocsStackedSnippets from "@components/DocsStackedSnippets"
import PreviewBlock from "@components/PreviewBlock.astro"
import Table from "@components/Table.astro"

import { Overview, overviewCode } from "@react-ui/tabs/Overview"
import { BottomIndicator, bottomIndicatorCode } from "@react-ui/tabs/BottomIndicator"
import { SoftToSoft, softToSoftCode } from "@react-ui/tabs/SoftToSoft"
import { SoftToSolid, softToSolidCode } from "@react-ui/tabs/SoftToSolid"
import { OutlinedToSolid, outlinedToSolidCode } from "@react-ui/tabs/OutlinedToSolid"
import { WithIconBadge, withIconBadgeCode } from "@react-ui/tabs/Custom"
import { listProps, indicatorProps } from "@react-ui/tabs/reference"
import { installation, usage } from "@react-ui/tabs/codeSnippets"

export const components = {
    ..._components
}

<div className="space-y-20">

    <PreviewBlock
        codeBlocks={overviewCode}
    >
        <Overview client:visible/>
    </PreviewBlock>


    <div>
        ## Installation

        Install the primitive and Copy-Paste the component code in a `.tsx` file.

        <DocsStackedSnippets snippets={installation} client:visible/>
    </div>

    <div>
        ## Usage

        Import all the parts and build your Tabs.
        
        <DocsStackedSnippets snippets={usage} client:visible/>
    </div>

    <div>
        ## Reference

        <div className="mt-6 space-y-12 mb-12"> 
            <div>
                ### List 

                The parent component of the tabs triggers

                <Table data={listProps} />
            </div>

            <div>
                ### Indicator

                The element that indicates the current tab

                <Table data={indicatorProps} />
            </div>
        </div>
    </div>

    <div>
        ## Examples

        <div className="mt-6 space-y-12">
            <div>
                ### Bottom Indicator

                <PreviewBlock codeBlocks={bottomIndicatorCode}>
                    <BottomIndicator client:visible/>
                </PreviewBlock>
            </div>

            <div>
                ### Soft To Soft

                <PreviewBlock codeBlocks={softToSoftCode}>
                    <SoftToSoft client:visible/>
                </PreviewBlock>
            </div>

            <div>
                ### Soft To Solid

                <PreviewBlock codeBlocks={softToSolidCode}>
                    <SoftToSolid client:visible/>
                </PreviewBlock>
            </div>

            <div>
                ### Outlined To Solid

                <PreviewBlock codeBlocks={outlinedToSolidCode}>
                    <OutlinedToSolid client:visible/>
                </PreviewBlock>
            </div>

            <div>
                ### With Icon and Badge

                <PreviewBlock codeBlocks={withIconBadgeCode}>
                    <WithIconBadge client:visible/>
                </PreviewBlock>
            </div>
        </div>
    </div>
</div>