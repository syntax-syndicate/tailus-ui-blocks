---
title: Tooltip
description: A popup that displays information related to an element when the element receives keyboard focus or the mouse hovers over it.
priorityOrder : 60
frameworkDocs : 
    name : Radix UI
    link : https://www.radix-ui.com/primitives/docs/components/tooltip#api-reference
---

import _components from "@content/_components"
import DocsStackedSnippets from "@components/DocsStackedSnippets"
import PreviewBlock from "@components/PreviewBlock.astro"
import Table from "@components/Table.astro"

import { Overview, overviewCode } from "@react-ui/tooltip/Overview"
import { contentProps } from "@react-ui/tooltip/reference"
import { Inverted, invertedCode } from "@react-ui/tooltip/Inverted"
import { Fancy, fancyCode } from "@react-ui/tooltip/Fancy"
import { installation, usage } from "@react-ui/tooltip/codeSnippets"

export const components = {
    ..._components
}

<div className="space-y-20">

    <PreviewBlock
        codeBlocks={overviewCode}
    >
        <Overview client:visible/>
    </PreviewBlock>


    <div>
        ## Installation

        Install the primitive and Copy-Paste the component code in a `.tsx` file.

        <DocsStackedSnippets snippets={installation} client:visible/>
    </div>

    <div>
        ## Usage

        Import all the parts and build your Dialog.
        
        <DocsStackedSnippets snippets={usage} client:visible/>
    </div>

    <div>
        ## Reference

        <div className="mt-6 space-y-12 mb-12"> 
            <div>
                ### Content 

                The content of the `Tooltip` component

                <Table data={contentProps} />
            </div>
        </div>
    </div>

    <div>
        ## Examples

        <div className="mt-6 space-y-12">
            <div>
                ## Inverted Tooltip

                <PreviewBlock codeBlocks={invertedCode}>
                    <Inverted client:visible/>
                </PreviewBlock>
            </div>

            <div>
                ## Fancy Tooltip

                <PreviewBlock codeBlocks={fancyCode}>
                    <Fancy client:visible/>
                </PreviewBlock>
            </div>
        </div>
    </div>
</div>