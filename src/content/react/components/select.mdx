---
title: Select
description: Displays a list of options for the user to pick from—triggered by a button.
priorityOrder : 50
frameworkDocs : 
    name : Radix UI
    link : https://www.radix-ui.com/primitives/docs/components/select#api-reference
---

import _components from "@content/_components"
import DocsStackedSnippets from "@components/DocsStackedSnippets"
import ComponentPreview from "@components/ComponentPreview"
import PreviewBlock from "@components/PreviewBlock.astro"
import CodeSnippet from "@components/utilities/CodeSnippet"
import Table from "@components/Table.astro"

import { Overview, overviewCode } from "@react-ui/select/Overview"
import * as reference from "@react-ui/select/reference"
import { installation, usage } from "@react-ui/select/codeSnippets"

export const components = {
    ..._components
}

<div className="space-y-20">

    <PreviewBlock
        codeBlocks={overviewCode}
    >
        <Overview client:visible/>
    </PreviewBlock>


    <div>
        ## Installation

        Install the primitive and Copy-Paste the component code in a `.tsx` file.

        <DocsStackedSnippets snippets={installation} client:visible/>
    </div>

    <div>
        ## Usage

        Import all the parts and build your Select.
        
        <DocsStackedSnippets snippets={usage} client:visible/>
    </div>

    <div>
        ## Reference

        <div className="mt-6 space-y-12 mb-12"> 
            <div>
                ### Trigger 

                The trigger of the `Select` component

                <Table data={reference.triggerProps} />
            </div>
            <div>
                ### Content 

                The content of the `Select` component

                <Table data={reference.contentProps} />
            </div>
            <div>
                ### Item 

                The item of the `Select` component, which is the option. By default, its appearance (defined by `variant` and `intent` props) is determined by the parent `Select.Content` component.

                <Table data={reference.itemProps} />
            </div>
            <div>
                ### Icon 

                The icon of the `Select.Trigger` component

                <Table data={reference.iconProps} />
            </div>
            <div>
                ### Item Trigger 

                The icon to indicate the selected item

                <Table data={reference.itemIndicatorProps} />
            </div>
        </div>
    </div>

    <div>
        ## Examples

        <div className="space-y-12">
            <div>
                
            </div>
        </div>
    </div>
</div>