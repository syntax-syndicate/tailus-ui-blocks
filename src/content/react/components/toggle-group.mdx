---
title: Toggle Group
description: A set of two-state buttons that can be toggled on or off.
priorityOrder : 57
frameworkDocs : 
    name : Radix UI
    link : https://www.radix-ui.com/primitives/docs/components/toggle-group#api-reference
---

import _components from "@content/_components"
import DocsStackedSnippets from "@components/DocsStackedSnippets"
import PreviewBlock from "@components/PreviewBlock.astro"
import Table from "@components/Table.astro"

import { Overview, overviewCode } from "@react-ui/toggle-group/Overview"
import { Formatting, formattingCode } from "@react-ui/toggle-group/Formatting"
import { Alignment, alignmentCode } from "@react-ui/toggle-group/Alignment"
import { Feedback, feedbackCode } from "@react-ui/toggle-group/Feedback"
import { rootProps, itemProps, iconProps } from "@react-ui/toggle-group/reference"
import { installation, usage } from "@react-ui/toggle-group/codeSnippets"

export const components = {
    ..._components
}

<div className="space-y-20">

    <PreviewBlock
        codeBlocks={overviewCode}
    >
        <Overview client:visible/>
    </PreviewBlock>


    <div>
        ## Installation

        Install the primitive and Copy-Paste the component code in a `.tsx` file.

        <DocsStackedSnippets snippets={installation} client:visible/>
    </div>

    <div>
        ## Usage

        Import all the parts and build your Toggle Group.
        
        <DocsStackedSnippets snippets={usage} client:visible/>
    </div>

    <div>
        ## Reference

        <div className="mt-6 space-y-12 mb-12"> 
            <div>
                ### Root 

                The parent component of the `ToggleGroup` component

                <Table data={rootProps} />
            </div>

            <div>
                ### Item 

                The item of the `ToggleGroup` component

                <Table data={itemProps} />
            </div>

            <div>
                ### Icon

                The Icon of the Toggle Item
                <Table data={iconProps} />
            </div>
        </div>
    </div>

    <div>
        ## Examples

        <div className="mt-6 space-y-12">
            <div>
                ### User Feedback

                <PreviewBlock codeBlocks={feedbackCode}>
                    <Feedback client:visible/>
                </PreviewBlock>
            </div>
            <div>
                ### Text Formatting

                <PreviewBlock codeBlocks={formattingCode}>
                    <Formatting client:visible/>
                </PreviewBlock>
            </div>
            <div>
                ### Text Alignment

                <PreviewBlock codeBlocks={alignmentCode}>
                    <Alignment client:visible/>
                </PreviewBlock>
            </div>
        </div>
    </div>
</div>