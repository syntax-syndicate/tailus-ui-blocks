---
title: Installation
description: Learn how to install Tailus Themer and all its dependancies in your project.
priorityOrder : 2
---

import _components from "@content/_components"
import DocsCodeStep from "@components/DocsCodeStep"

export const components = {
    ..._components
}

export const importThemerPackages = `import { outlinedForm as formTheme } from "@tailus/themer-form";
import { button as buttonTheme} from "@tailus/themer-button";
import { card as cardTheme } from "@tailus/themer-card";
`

export const applyingThemer = `import * as Form from "@radix-ui/react-form";

import { outlinedForm as formTheme } from "@tailus/themer-form";
import { button as buttonTheme} from "@tailus/themer-button";
import { card as cardTheme } from "@tailus/themer-card";

const FormUI = () => (
    <Form.Root className={cardTheme}>
        <div className="space-y-4">
            <Form.Field className={formTheme.field} name="email">
                <Form.Label className={formTheme.label.md}>Email</Form.Label>
                <div className="space-y-1">
                    <Form.Control asChild>
                        <input className={formTheme.input.md} type="email" required />
                    </Form.Control>
                    <Form.Message className={formTheme.message.warning} match="valueMissing">
                        Please enter your email
                    </Form.Message>
                    <Form.Message className={formTheme.message.danger} match="typeMismatch">
                        Please provide a valid email
                    </Form.Message>
                </div>
            </Form.Field>
            <Form.Field className={formTheme.field} name="question">
                <Form.Label className={formTheme.label.md}>Question</Form.Label>
                <div>
                    <Form.Control asChild>
                        <textarea className={formTheme.textarea.md} rows={3} required />
                    </Form.Control>
                    <Form.Message className={formTheme.message.warning} match="valueMissing">
                        Please enter a question
                    </Form.Message>
                </div>
            </Form.Field>
            <Form.Submit asChild>
                <button className={button.primary.md + " w-full"}>
                    <span>Submit</span>
                </button>
            </Form.Submit>
        </div>
    </Form.Root>
);

export default FormUI;
`
export const installation = {
    code : "npm install @tailus/themer",
    lang:"tsx",
    file:"terminal",
    filename:"Terminal",
    isCentered:true
}

export const usage = {
    code : `import themer from "@tailus/themer";

export default = {
    content : [
        // ... your other paths here
        "./node_modules/@tailus/themer-**/dist/**/*.{js,ts}"
    ],
    theme: {
        extend: {},
    },
    plugins: [
        themer
    ],
};
    
    `,
    lang:"tsx",
    file:"tailwind",
    filename:"tailwind.config.js",
}

export const componentsInstallation = {
    code : `npm install @tailus/themer-form @tailus/themer-button @tailus/themer-card`,
    lang:"tsx",
    file:"terminal",
    filename:"Terminal",
    isCentered:true
}

export const formInstallation = {
    code : `npm install @radix-ui/react-form`,
    lang:"tsx",
    file:"terminal",
    filename:"Terminal",
    isCentered:true
}

export const usingPrimitiveWithThemer = {
    code : importThemerPackages,
    lang:"tsx",
    file:"tsx",
    filename:"Form.tsx",
    highlightedLines:"3-5",
    isCentered:true
}

export const stylingPrimitive = [
    {
        code : applyingThemer,
        lang:"tsx",
        file:"tsx",
        filename:"Form.tsx",
        highlightedLines:"8-36",
    }
]

<div>
    ## Prerequisites
    Before you begin, ensure that you have already installed Tailwind CSS in your project. If not, please follow the [Tailwind CSS installation guide](https://tailwindcss.com/docs/installation).
</div>

<div>
    <div className="space-y-8 md:space-y-16">
        <div>
            ### 1. Install the themer package
            <div className="mt-6">
                <DocsCodeStep
                    {...installation}
                    client:visible
                >
                    Run the following command in your terminal to install the Tailus Themer package in your project:
                </DocsCodeStep>
                This command will add the `@tailus/themer` package to your project's dependencies.
            </div>
        </div>

        <div>
            ### 2. Import and use the themer plugin
            <div className="mt-6">
                <DocsCodeStep
                    {...usage}
                    client:visible
                    className="mb-10"
                >
                    After installing the Themer package, import and use the `themer` plugin in your Tailwind CSS configuration file:
                </DocsCodeStep>
                In this code snippet, the `themer` plugin is imported at the top of the file. Then, the path to the Themer packages is added to the `content` array, and the `themer` plugin is added to the `plugins` array in your Tailwind CSS configuration file.
            </div>
        </div>

        <div>
            ### 3. Install components Themer packages
            Install the Themer packages for the components you want to use in your project. For example, if you want to use the `Form` component, install the `@tailus/themer-form` package.
            
            To learn more about a component's Themer package, see its documentation.
        </div>
    </div>
</div>